@model IEnumerable<courierf.Models.Branch>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Courier.Courier_type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Branch_pin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Branch_address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Branch_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Courier.Courier_type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Branch_pin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Branch_address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Branch_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Branch_code }) |
            @Html.ActionLink("Details", "Details", new { id=item.Branch_code }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Branch_code })
        </td>
    </tr>
}

</table>
